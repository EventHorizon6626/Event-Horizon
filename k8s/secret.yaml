apiVersion: v1
kind: Secret
metadata:
  name: event-horizon-secrets
  namespace: event-horizon
type: Opaque
stringData:
  # Replace with your actual API key
  NEWS_API_KEY: "your-news-api-key-here"

  # Add other secrets as needed
  # DB_PASSWORD: "your-db-password"
  # REDIS_PASSWORD: "your-redis-password"

---
# Alternative: Use external secrets (recommended for production)
# Uncomment and configure for AWS Secrets Manager, GCP Secret Manager, etc.
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: event-horizon-external-secret
#   namespace: event-horizon
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: SecretStore
#   target:
#     name: event-horizon-secrets
#   data:
#   - secretKey: NEWS_API_KEY
#     remoteRef:
#       key: event-horizon/news-api-key
